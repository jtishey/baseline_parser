---
- show_bfd_session:
  command:  show bfd session
  ignore-null: False
  iterate: [all]
  blacklist: [Interface, Detect, Cumulative, transmit, sessions]
  tests:
    - no-diff: [2, 1, 0]
      info: "PASSED! BFD session to {{ pre[2] }} to {{ pre[0] }} is still {{ pre[1] }}"
      err: "FAILED! BGP session to {{ pre[2] }} changed from {{ pre[1] }}:{{ pre[2] }} to {{ post[1] }}:{{ post[2] }}"
