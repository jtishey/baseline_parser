---
- show_route_summary:
  command: show route summary
  ignore-null: False
  iterate: ['inet']
  blacklist: []
  tests:
    - no-diff: [0, 3]
      info: "PASSED! {{ pre[0] }} still has {{ pre[3] }} routes"
      err: "FAILED! {{ pre[0] }} changed from {{ pre[3] }} to {{ post[3] }} routes"